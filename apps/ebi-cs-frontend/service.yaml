apiVersion: v1
kind: Service
metadata:
  name: ebi-cs-frontend-service
  namespace: ebi-cs-api
spec:
  type: ClusterIP
  selector:
    app: ebi-cs-frontend
  ports:
    - port: 80
      targetPort: 80
