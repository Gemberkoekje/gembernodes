apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ebi-cs-frontend
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ebi-cs-frontend
  policy:
    semver:
      range: "0.x.x-*"
