apiVersion: v1
kind: Service
metadata:
  name: hackerminigames-service
  namespace: hackerminigames
spec:
  type: ClusterIP
  selector:
    app: hackerminigames
  ports:
    - port: 80
      targetPort: 8080
