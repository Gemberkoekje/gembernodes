apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ebi-cs-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ebi-cs-api
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^1\.0\.0-(?P<build>\d+)$'
    extract: '$build'
