apiVersion: v1
kind: Service
metadata:
  name: dockhand
  namespace: dockhand
spec:
  selector:
    app: dockhand
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: LoadBalancer
  loadBalancerIP: 192.168.1.231
