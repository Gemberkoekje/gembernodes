apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-qnap
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: qnap-nfs
  resources:
    requests:
      storage: 2Gi
