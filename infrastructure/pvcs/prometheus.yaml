apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-server-qnap
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: qnap-nfs
  resources:
    requests:
      storage: 2Gi