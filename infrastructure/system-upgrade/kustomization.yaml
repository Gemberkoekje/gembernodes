apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Pinned: `releases/latest` applied whatever was released upstream on the next reconcile,
  # with cluster-wide permissions. Bump this deliberately (v0.20.2 is what was running).
  - https://github.com/rancher/system-upgrade-controller/releases/download/v0.20.2/system-upgrade-controller.yaml
